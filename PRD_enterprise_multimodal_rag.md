# 企业级多模态 RAG 系统 — 产品需求文档 (PRD)

> 版本：v0.2（RAG 检索专家视角修订）
> 日期：2026-03-31
> 侧重：**稳定性 · 可用性**

---

## 一、项目背景与目标

### 1.1 背景

企业内部积累了大量多模态文档（PDF、Word、PPT、Excel、图片），现有搜索系统仅支持关键词匹配，无法理解语义，更无法支持需要跨文档推理的复杂问题。

### 1.2 目标

构建一套企业级多模态 RAG 系统，让员工能以自然语言提问，获得有来源依据的精准回答，并支持**串联式对话问答**（多轮追问、上下文累积推理）。

### 1.3 核心定位

> **可审计的企业知识供应链**，不是聊天机器人。

每一个回答必须可溯源到原始文档的具体位置，错误必须可追责。

---

## 二、用户与场景

### 2.1 主要用户

| 角色 | 典型诉求 |
|------|---------|
| 业务人员 | 快速找到政策/合同/产品规格，无需翻多个系统 |
| 分析师 | 跨文档数据整合，支持串联追问（"项目背景→进展→结论"） |
| 管理层 | 摘要提炼 + 数据对比，快速决策 |
| IT/知识库管理员 | 管理文档入库、监控系统健康、维护知识质量 |

### 2.2 核心场景

**场景 A — 串联式多轮问答（核心场景）**

```
用户：2024年我们在华南的销售情况怎么样？
系统：华南区2024年总销售额 XX 万，同比增长 YY%。[来源：Q4销售报告 P3]

用户：主要增长来自哪个产品线？          ← 隐式指代"华南区增长"
系统：主要增长来自 Z 产品线，贡献了 65% 的增量。[来源：产品线分析 P7]

用户：Z 产品线的客户投诉情况如何？      ← 继续累积上下文
系统：Z 产品线在华南区共收到 XX 个有效投诉... [来源：客服月报 2024-12]
```

每一轮：Query Reformulation 将含指代的问题改写为独立完整查询，再送入检索管道。

**场景 B — 跨格式文档检索**

答案分布在 PDF 表格、PPT 数据图、Word 说明文档中，系统综合多源内容给出完整回答，标注每个事实的具体来源。

**场景 C — 文档更新后即时可用**

文件上传后，分钟级内可被检索到，旧版本内容在新版本入库后自动降权。

---

## 三、系统架构总览

```
┌─────────────────────────────────────────────────────────────────┐
│                          用户接入层                              │
│            Web Chat UI  /  REST API  /  企业系统集成             │
└────────────────────────────┬────────────────────────────────────┘
                             │
┌────────────────────────────▼────────────────────────────────────┐
│                        对话编排层                                │
│    会话管理  →  Query Reformulation  →  子查询分解               │
│                         ↕  多跳循环（最多4跳）                   │
└──────────┬──────────────────────────────┬───────────────────────┘
           │                              │
┌──────────▼────────────┐    ┌────────────▼─────────────────────┐
│      检索管道（核心）   │    │           生成层                  │
│                        │    │  上下文组装（排序+截断）           │
│  ① 查询增强            │    │  Claude API（主）/ GPT-4o（备）   │
│  ② 混合检索            │    │  Citation 注入 / 流式输出         │
│     ├─ 稀疏(SPLADE)    │    │  幻觉抑制 Prompt                  │
│     └─ 稠密(向量)      │    └──────────────────────────────────┘
│  ③ RRF 融合            │
│  ④ 元数据预过滤        │
│  ⑤ BGE Reranker 精排   │
│  ⑥ 相关性阈值过滤      │
│  ⑦ 近重复去重          │    ← 新增，防止冗余 chunk 充斥上下文
└──────────┬────────────┘
           │   失败/空结果 → 降级链（见 §6）
┌──────────▼──────────────────────────────────────────────────────┐
│                         知识存储层                               │
│  向量库(Qdrant)   元数据库(PostgreSQL)   文件存储(OSS/MinIO)     │
│  ← 稠密向量 + 稀疏向量(SPLADE) + payload 元数据 统一存储        │
└──────────┬──────────────────────────────────────────────────────┘
           │
┌──────────▼──────────────────────────────────────────────────────┐
│                      文档处理层（异步 Pipeline）                  │
│  上传 → 消息队列(Redis) → 解析Worker → 清洗 → 分块 → Embedding  │
│  → 写入 Qdrant + PostgreSQL；失败进 Dead Letter Queue + 告警     │
└─────────────────────────────────────────────────────────────────┘
```

---

## 四、功能清单

### F1 — 文档处理与知识入库

#### F1.1 多格式文档解析

| 功能 | 描述 | 优先级 |
|------|------|--------|
| PDF 文字提取 | 含排版恢复，保留段落/标题层级 | P0 |
| PDF 表格提取 | 结构化提取，保留行列关系；失败时降级为文本提取并标注 ⚠️ | P0 |
| Word (.docx) 解析 | 提取正文/标题/表格/嵌入图片描述 | P0 |
| Excel (.xlsx) 解析 | 多 Sheet 结构化提取 + 数值保留；大表格按逻辑块切分 | P0 |
| PPT (.pptx) 解析 | 文字提取 + 每页截图送 VLM 生成语义描述 | P1 |
| 图片内容理解 | 调用 Claude Vision 生成描述，描述文本写入索引，原图保留 | P1 |
| 扫描件 PDF | OCR（Azure Document Intelligence 优先，PaddleOCR 备选）| P1 |
| 图表语义提取 | VLM 将折线图/柱状图转为数值描述 + 原图对照 | P2 |

> **解析质量保障**：每个 chunk 写入时附带 `parse_confidence` 分（0-1）。低于 0.7 的 chunk 在检索结果中标记 ⚠️，同时保存原始页面截图 URL 供用户点击验证。解析失败的文档进入失败队列，管理后台显示错误原因，支持手动重试。

#### F1.2 分块策略（RAG 质量天花板，必须精细设计）

> **专家注**：Chunking 策略是整个 RAG 系统质量的实际上限，优先级不低于检索算法。

**分块参数规范：**

| 文档类型 | 策略 | chunk 大小 | 重叠 (overlap) | 说明 |
|---------|------|-----------|---------------|------|
| 普通正文 PDF/Word | 语义段落边界 | 512 tokens | 64 tokens | 按段落/章节切，不按固定长度 |
| 表格 | 整表为单块 | ≤ 1024 tokens | 0 | 表格不拆分；超大表格按行组切 |
| PPT 幻灯片 | 一页一块 | ≤ 512 tokens | 0 | 附加幻灯片标题作前缀 |
| 长文档章节 | 章节感知切 | 800 tokens | 100 tokens | 章节标题必须包含在每个 chunk 内 |
| 图片/图表描述 | 描述整体为单块 | ≤ 256 tokens | 0 | 附加原文档名和位置元数据 |

**父子块结构（Small-to-Big Retrieval）：**

```
父块（800 tokens）← 用于生成时的上下文补全
  ├── 子块 A（200 tokens）← 用于检索精确匹配
  ├── 子块 B（200 tokens）
  └── 子块 C（200 tokens）
```

检索时用子块做向量匹配（精度高），返回给 LLM 时自动扩展为父块（上下文完整）。

**每个 chunk 必须携带的元数据字段：**

```json
{
  "doc_id": "uuid",
  "doc_name": "2024_Q4销售报告.pdf",
  "doc_type": "pdf",
  "page_num": 3,
  "section_path": "第二章 > 华南区数据",
  "chunk_index": 7,
  "parent_chunk_id": "uuid",
  "created_at": "2026-03-31T10:00:00Z",
  "doc_version": 2,
  "parse_confidence": 0.92,
  "content_type": "text|table|image_desc"
}
```

> `section_path` 和 `page_num` 是 Citation 功能的核心依赖，**必须 Day 1 实现**，后补极难。

#### F1.3 增量更新与版本管理

| 功能 | 描述 | 优先级 |
|------|------|--------|
| 新文档异步入库 | 上传后进入 Redis 队列，Worker 消费，分钟级完成索引 | P0 |
| 文档更新检测 | 重新上传同名文件时，基于文件哈希检测变更；有变更则标记旧向量 `is_latest=false`，入库新向量 | P0 |
| 文档删除同步 | 删除文件时，软删除 PostgreSQL 记录 + 从 Qdrant 删除对应向量 | P0 |
| 处理失败重试 | 解析/入库失败进 Dead Letter Queue，最多重试 3 次，超限告警管理员 | P0 |
| 全量索引重建 | 提供一键全量重建能力（Embedding 模型替换时使用），不影响在线服务 | P1 |
| 版本历史保留 | 保留旧版本向量，支持按版本检索（默认只查 `is_latest=true`）| P2 |

---

### F2 — 检索引擎（核心，侧重稳定性）

#### F2.1 混合检索

| 功能 | 描述 | 优先级 |
|------|------|--------|
| 稠密向量检索 | BGE-M3 或 text-embedding-3-large，HNSW 索引，top-K=20 | P0 |
| 稀疏检索（SPLADE）| **注意**：Qdrant 稀疏向量需要 SPLADE 模型生成稀疏向量，不是简单 BM25 关键词索引。推荐 SPLADE-v3 或 BM25-encoder | P0 |
| RRF 混合融合 | Reciprocal Rank Fusion，k=60（标准值）；稠密:稀疏权重可配置（默认 0.6:0.4）| P0 |
| 元数据预过滤 | **检索前** 按文档类型/时间范围/标签过滤（pre-filter，不是 post-filter）；pre-filter 效率更高且不泄漏信息 | P0 |

> **SPLADE vs BM25 说明**：Qdrant 的"稀疏向量"是学习型稀疏表示，需要 SPLADE 类模型推理生成，不是传统倒排索引 BM25。如果希望用传统 BM25，需要单独部署 Elasticsearch 或使用 Qdrant + BM25-encoder 方案。推荐统一用 Qdrant + SPLADE，减少运维复杂度。

#### F2.2 重排序（Reranking）

| 功能 | 描述 | 优先级 |
|------|------|--------|
| BGE-Reranker 精排 | BGE-Reranker-v2-m3，输入 top-20，输出 top-5；CPU 推理约 100-150ms | P0 |
| 相关性阈值过滤 | Reranker 分数 < 0.4 的块不送 LLM；阈值可配置，建议从 0.4 开始调校（非固定 0.65） | P0 |
| 近重复去重 | top-5 候选中，若两个 chunk 余弦相似度 > 0.92，仅保留分数更高的一个，防止冗余信息充斥 LLM 上下文 | P0 |
| 多模态内容联合排序 | 图片描述 chunk 与文字 chunk 统一参与 Reranker 排序 | P1 |

> **Reranker 硬件建议**：BGE-Reranker-v2-m3 在 4核 CPU 上，20条候选延迟约 100-200ms，可接受。如果 P95 延迟敏感，可部署 GPU 推理或使用更小的 BGE-Reranker-base。

#### F2.3 上下文组装（Context Assembly）

> 这是原文档缺失的关键步骤。检索到 chunk 后，如何组装送入 LLM 直接影响生成质量。

| 功能 | 描述 | 优先级 |
|------|------|--------|
| 父块扩展 | 检索命中子块后，自动扩展为父块（Small-to-Big），提供完整上下文 | P0 |
| 相关性排序入 prompt | 相关性最高的 chunk 放在 prompt 最后（Lost-in-the-Middle 效应：LLM 对中间内容注意力最弱） | P0 |
| Token 预算管理 | 所有 chunk 总 token 数不超过 LLM 上下文窗口的 50%，剩余留给对话历史和生成空间 | P0 |
| 跨文档去重 | 不同文档中的近似内容在组装时只保留一份（基于 chunk embedding 相似度）| P1 |

#### F2.4 查询增强

| 功能 | 描述 | 优先级 |
|------|------|--------|
| HyDE（假设文档嵌入）| 先用 LLM 生成假设答案，再对假设答案做向量检索；**注意**：增加 1 次 LLM 调用，延迟+成本上升，应作为可配置开关 | P1 |
| 查询扩展 | 同义词/缩写展开（配置领域词典，如"Q4"→"第四季度"）| P1 |
| 多语言对齐 | 中英文混合查询时，自动翻译为知识库主语言再检索 | P1 |

---

### F3 — 多轮对话与串联式多跳推理

#### F3.1 会话上下文管理

| 功能 | 描述 | 优先级 |
|------|------|--------|
| 多轮上下文保持 | 保留最近 10 轮对话（可配置），随每次请求传入 LLM | P0 |
| 隐式指代消解 | "它"、"这个产品"、"上面提到的" → Query Reformulation 步骤解析为明确实体 | P0 |
| 会话持久化 | 会话存储于 PostgreSQL，用户可恢复历史会话 | P1 |
| 会话摘要压缩 | 超过 10 轮时，将早期对话压缩为摘要（约 200 tokens），控制上下文总长度 | P1 |

#### F3.2 Query Reformulation（串联推理核心）

**执行流程：**

```
原始问题 + 对话历史
        ↓
LLM 重写 → 独立完整的检索查询
        ↓
检查重写质量（长度/语义完整性）
        ↓
  通过 → 进入检索管道
  失败 → 降级：用原始问题直接检索（保障可用性）
```

| 功能 | 描述 | 优先级 |
|------|------|--------|
| 查询重写 | 将含指代/省略的问题改写为独立完整查询，不改变语义 | P0 |
| 重写失败降级 | 重写结果为空或明显异常（< 5 tokens 或 > 原问题 3 倍长度）时，使用原始问题降级检索 | P0 |
| 子查询分解 | 复合问题拆为 2-3 个子查询并行检索，结果合并 | P1 |
| 检索-生成交替 | 每跳的检索结果作为下一跳的查询上下文（IRCoT 简化版，最多 4 跳）| P1 |

#### F3.3 多跳推理编排

| 功能 | 描述 | 优先级 |
|------|------|--------|
| 最多 4 跳 | 系统自主判断是否追加检索，硬上限 4 跳；超限时基于已有内容生成，并提示用户信息可能不完整 | P1 |
| 中间结果验证 | 每跳检索后验证相关性；若 Reranker top-1 分 < 0.4，终止推理，不继续下一跳 | P1 |
| 推理链可视化 | 用户可展开查看每一跳的查询和检索来源 | P2 |

> **多跳错误放大说明**：单跳检索准确率 85%，四跳端到端准确率约 0.85⁴ ≈ 52%。因此多跳功能应配合中间结果验证使用，宁可在某跳中止并告知用户，也不继续传递低质量中间结果。

---

### F4 — 生成与回答质量

#### F4.1 有据回答（Citation First）

| 功能 | 描述 | 优先级 |
|------|------|--------|
| 强制引用标注 | System Prompt 要求 LLM 用 `[来源：文档名 P页码]` 格式标注每个事实 | P0 |
| 溯源跳转 | 点击引用标注，跳转至原文档对应页面（URL 由后端生成）| P0 |
| 无据拒答 | 有效 chunk 数为 0 时，返回"知识库中暂无相关内容，请确认问题范围"；不猜测生成 | P0 |
| 低置信度提示 | Reranker top-1 分在 0.4-0.6 之间时，生成回答但附加"⚠️ 检索到的内容相关性较低，请核实" | P0 |
| 多源综合标注 | 答案来自多个文档时，每个来源分别标注 | P0 |

> **与原版差异**：将"低质量检测"从 P1 升为 P0，且细化为两档处理——完全无结果时拒答，低置信度时提示而不拒答，兼顾稳定性和可用性。

#### F4.2 回答格式

| 功能 | 描述 | 优先级 |
|------|------|--------|
| Markdown 渲染 | 表格、列表、代码块 | P0 |
| 流式输出（SSE）| 打字机效果，首 token 延迟目标 < 1s | P0 |
| 检索失败时的建议 | 无结果时，推荐 3 条相关问题（基于知识库内容生成）| P1 |
| 摘要模式 | 长文档自动精简回答 | P1 |
| 对比模式 | 明确对比两个实体时自动生成对比表格 | P2 |

#### F4.3 幻觉抑制

```
System Prompt 核心约束：
1. 只基于 <context> 标签内的内容回答
2. 不允许推断 context 中未出现的事实
3. 每个具体事实必须以 [来源：X] 格式标注
4. 无法从 context 找到答案时，明确说"根据现有知识库无法回答"
5. 不允许说"根据我的知识/通常来说..."
```

---

### F5 — 稳定性设计（新增章节）

> 这是侧重稳定性与可用性的核心章节，原文档缺失。

#### F5.1 降级链（Fallback Chain）

每个关键组件必须有降级策略，保证单点故障不导致全链路不可用：

```
正常路径：稠密+稀疏混合检索 → RRF → Reranker → 生成
                                                    
降级 L1：Reranker 超时/不可用 → 跳过重排，直接用 RRF top-5 结果生成（标注"精排服务暂不可用"）
降级 L2：Qdrant 稀疏索引不可用 → 只用稠密向量检索
降级 L3：Qdrant 完全不可用 → 返回"检索服务暂时不可用，请稍后重试"（不生成幻觉答案）
降级 L4：LLM API 超时（> 30s）→ 返回原始 top-3 chunk 内容，提示"AI 生成超时，以下为原文摘录"
降级 L5：Query Reformulation 失败 → 用原始问题直接检索
```

| 功能 | 描述 | 优先级 |
|------|------|--------|
| Reranker 降级 | Reranker 服务超时（> 500ms）时，跳过重排，记录告警 | P0 |
| LLM API 熔断 | 连续 3 次失败触发熔断，30s 内不再调用，返回降级响应 | P0 |
| 向量库降级 | Qdrant 不可用时，降级返回错误而非产生幻觉 | P0 |
| 查询降级 | Query Reformulation 异常时，原始问题兜底 | P0 |

#### F5.2 检索质量护栏（Guardrails）

| 功能 | 描述 | 优先级 |
|------|------|--------|
| 相关性硬阈值 | Reranker top-1 分 < 0.4 时拒答；0.4-0.6 时给出但标注低置信度 | P0 |
| 最少有效 chunk 数 | 有效 chunk（通过阈值过滤）为 0 时，不调用 LLM，直接返回无结果提示 | P0 |
| 上下文 Token 上限 | 送入 LLM 的 chunk 总 token 数不超过模型上下文窗口的 50% | P0 |
| 重复内容上限 | 若 top-5 中有 3 个以上 chunk 来自同一文档同一页，主动提示"答案主要来源于单一文档，可能缺乏全面性" | P1 |

#### F5.3 异步处理稳定性

| 功能 | 描述 | 优先级 |
|------|------|--------|
| Dead Letter Queue | 文档处理失败 3 次后进入 DLQ，触发告警，不阻塞正常队列 | P0 |
| 幂等处理 | 同一文档重复入队时，基于文件哈希去重，不重复处理 | P0 |
| 处理超时控制 | 单文档处理超时（> 10 分钟）时强制终止，标记失败，释放 Worker | P0 |
| 批量 Embedding | 分块后批量调用 Embedding API（batch_size=64），减少 API 调用次数和成本 | P1 |
| 背压控制 | 队列积压超过阈值时，暂停新文档入队，告警管理员 | P1 |

---

### F6 — 知识库管理（后台）

#### F6.1 文档管理

| 功能 | 描述 | 优先级 |
|------|------|--------|
| 文档上传（批量/单个）| 支持拖拽、文件夹批量上传，单文件限制可配置（默认 50MB）| P0 |
| 上传进度与状态 | 实时显示：排队中/解析中/入库中/完成/失败（含错误原因）| P0 |
| 文档列表与搜索 | 按名称、类型、上传时间、处理状态筛选 | P0 |
| 文档删除 | 软删除 + 异步清理索引；删除操作需二次确认 | P0 |
| 失败文档重试 | 管理员可对失败文档手动触发重新处理 | P0 |
| 文档标签管理 | 手动打标签，用于检索 pre-filter | P1 |
| 文档预览 | 在线预览原始文档，支持跳转到指定页 | P1 |

#### F6.2 全链路检索可观测性

> **专家注**：RAG 系统的可观测性不能只记录"用户查了什么"，必须记录"检索到了什么、打分是多少、最终用了哪些 chunk"，这是调优和 Debug 的基础。

每次检索请求必须记录：

```json
{
  "session_id": "...",
  "query_original": "这个产品的客户评价呢？",
  "query_reformulated": "Z产品线在华南区2024年的客户投诉情况",
  "retrieval": {
    "dense_top20": ["chunk_id_1", "..."],
    "sparse_top20": ["chunk_id_3", "..."],
    "after_rrf_top10": ["chunk_id_1", "chunk_id_3", "..."],
    "after_reranker_top5": [
      {"chunk_id": "...", "score": 0.87, "doc": "客服月报.pdf", "page": 7},
      {"chunk_id": "...", "score": 0.71, "doc": "产品反馈汇总.xlsx", "page": 2}
    ],
    "chunks_sent_to_llm": 3,
    "total_tokens": 1240
  },
  "generation": {
    "model": "claude-3-5-sonnet",
    "prompt_tokens": 1800,
    "completion_tokens": 320,
    "latency_ms": 3200
  },
  "user_feedback": null
}
```

| 功能 | 描述 | 优先级 |
|------|------|--------|
| 检索全链路日志 | 记录上述结构化日志，可按 session/query 查询 | P0 |
| 低质量检索告警 | 检索到的 top-1 分数连续低于阈值时，自动告警 | P1 |
| 检索质量 Dashboard | Precision@3、MRR、无结果率、用户满意度趋势 | P1 |
| 文档健康检查 | 定期检测解析置信度低、无被检索记录（孤岛文档）的文档 | P1 |
| API 用量与成本统计 | LLM/Embedding/Reranker 调用量、Token 消耗、成本（按天/周/月）| P1 |

#### F6.3 评估体系

| 功能 | 描述 | 优先级 |
|------|------|--------|
| Golden Set 管理 | 维护"问题-标准答案-来源文档"评估集（至少 50 条，覆盖主要业务场景）| P1 |
| 自动评估跑批 | 每周对 Golden Set 跑 RAG，计算 Precision@3、MRR、Answer F1 | P1 |
| 用户反馈收集 | 每条回答支持点赞/踩 + 可选文字反馈 | P1 |
| 人工抽检工作台 | 从真实查询日志中抽取样本供人工评分，结果写入评估历史 | P2 |

---

## 五、技术选型

| 层次 | 选型 | 理由 |
|------|------|------|
| **文档解析** | Unstructured.io（核心）+ Azure Document Intelligence（扫描件/复杂表格）+ Claude Vision（图表/图片描述） | Unstructured 覆盖主流格式；Azure 处理扫描件质量最优；Vision 模型兜底多模态 |
| **文本分块** | LlamaIndex SentenceSplitter + 自定义父子块 NodeParser | 语义感知、支持父子块、可扩展 |
| **稠密 Embedding** | text-embedding-3-large（首选）/ BGE-M3（中英双语备选）| 向量只是"缓存"，原始文本必须保留，模型可替换 |
| **稀疏 Embedding** | SPLADE-v3 / BM25-encoder | Qdrant 稀疏向量依赖稀疏模型，非传统 BM25 关键词索引 |
| **向量数据库** | Qdrant | 统一存储稠密+稀疏向量、支持 payload 过滤、开源自建、Rust 实现性能稳定 |
| **重排序** | BGE-Reranker-v2-m3（本地 CPU 部署）| 开源、中文友好、延迟可控（100-200ms/20条）|
| **生成模型** | Claude 3.5 Sonnet（主力）+ GPT-4o（熔断备用）| Claude 长文档理解和遵循引用指令能力最优 |
| **对话编排** | 自研轻量 Pipeline（MVP）→ LangGraph（多跳 Agentic 流扩展）| 轻量 Pipeline 可控性强，适合稳定性优先场景；LangGraph 用于 Phase 3+ |
| **后端框架** | Python + FastAPI + asyncio | AI 生态最完整，异步支持好 |
| **任务队列** | Celery + Redis（队列 + 结果存储 + 缓存）| 文档处理异步、DLQ、背压控制 |
| **元数据存储** | PostgreSQL | 文档元信息、会话历史、检索日志、评估数据 |
| **文件存储** | 阿里云 OSS / MinIO（私有化）| 原始文件持久化，生成预签名 URL 支持 Citation 跳转 |
| **前端** | Next.js + Tailwind CSS | Chat UI 生态成熟，SSE 流式输出支持好 |

---

## 六、MVP 交付路径

### Phase 1 — 核心可用（第 1-3 周）

**目标**：单轮问答可用，稳定性优先，P0 功能全覆盖。

- [ ] PDF + Word 解析（纯文字，不含图表）+ 语义分块（含父子块）
- [ ] 稠密向量检索 + BGE Reranker + 相关性阈值过滤
- [ ] 无据拒答 + 低置信度提示（两档处理）
- [ ] Claude API 生成 + 强制 Citation 标注 + 溯源跳转
- [ ] Reranker/LLM 降级链（L1-L4）
- [ ] 检索全链路日志
- [ ] 文档上传 + 异步处理 + DLQ
- [ ] 基础 Web Chat UI（单轮，流式输出）

**Phase 1 完成标准**：100 篇 PDF 文档入库，10 个预设问题的 Precision@3 ≥ 0.7，系统无崩溃连续运行 72 小时。

---

### Phase 2 — 对话与混合检索（第 4-5 周）

**目标**：串联式多轮对话 + 混合检索，回答质量显著提升。

- [ ] 多轮会话上下文管理 + 会话持久化
- [ ] Query Reformulation（指代消解 + 重写失败降级）
- [ ] SPLADE 稀疏向量 + RRF 混合检索
- [ ] 近重复 chunk 去重
- [ ] 上下文组装（Lost-in-the-Middle 排序 + Token 预算管理）
- [ ] 子查询分解（2跳）

**Phase 2 完成标准**：3-5 轮串联追问场景测试通过，Precision@3 ≥ 0.75，P95 响应延迟 ≤ 8s。

---

### Phase 3 — 多模态与质量体系（第 6-8 周）

**目标**：多模态文档支持，建立质量闭环。

- [ ] Excel 表格提取 + PPT 解析 + Claude Vision 图表描述
- [ ] 扫描件 OCR（Azure Document Intelligence）
- [ ] Golden Set 建立 + 自动评估跑批
- [ ] 用户反馈收集 + 检索质量 Dashboard
- [ ] 文档健康检查 + API 成本统计
- [ ] 查询建议（无结果时推荐相关问题）

**Phase 3 完成标准**：多模态文档入库，用户满意度（点赞率）≥ 0.65，评估体系上线。

---

### Phase 4 — 稳定化与规模化（第 9-12 周）

**目标**：系统可观测、可运维，支持大规模文档。

- [ ] 全量索引重建能力（不中断服务）
- [ ] HyDE 查询增强（可配置开关）
- [ ] 4跳自动推理 + 推理链可视化
- [ ] 背压控制 + 队列深度监控
- [ ] 批量 Embedding 优化（降低入库成本）
- [ ] LangGraph 多跳编排替换轻量 Pipeline

---

## 七、关键设计决策与风险

### 7.1 必须 Day 1 实现

1. **向量存原始文本块**：Embedding 视为"缓存"，模型换了只需重新 Embedding，不丢数据
2. **每个 chunk 存完整元数据**（doc_id / page_num / section_path）：Citation 功能依赖，后补极难
3. **降级链（F5.1）**：组件失败时有明确的降级路径，稳定性优先
4. **检索全链路日志**：没有可观测性就无法调优，上线第一天就要记录

### 7.2 主要风险

| 风险 | 概率 | 缓解方案 |
|------|------|---------|
| 表格/复杂 PDF 解析质量不达标 | 高 | 附原始截图、置信度标注；Azure Document Intelligence 作为高质量备选 |
| SPLADE 稀疏向量推理增加延迟 | 中 | 入库时离线预计算稀疏向量，检索时只做查询侧稀疏推理（< 20ms） |
| Query Reformulation 改写语义偏移 | 中 | 改写后与原始查询做相似度校验（> 0.6），差距过大时使用原始查询降级 |
| LLM API 成本超预期（多跳场景）| 中 | 多跳上限 4 次；HyDE 默认关闭；启用检索结果缓存（相同 query hash 复用）|
| Embedding 模型被弃用 | 低 | 原始文本块保留，可随时全量重新 Embedding；全量重建不影响在线服务 |
| 多跳错误放大 | 中 | 每跳中间结果验证，低质量直接终止；用户界面展示推理链便于排查 |

### 7.3 不做的事（刻意排除）

- ❌ **知识图谱**：构建和维护成本远超收益；80% 多跳场景用 Query Decomposition 可解
- ❌ **秒级实时索引**：分钟级对 95% 场景足够；秒级架构复杂度代价不值
- ❌ **自定义 Embedding 微调**：PMF 验证前的过度投入
- ❌ **多租户权限隔离**：当前统一知识库；若后续需要，在 Qdrant collection 层面隔离

---

## 八、评估指标（上线后持续监控）

| 指标 | 目标值 | 说明 |
|------|--------|------|
| Precision@3 | ≥ 0.75 | 返回的前3个 chunk 中，相关的比例 |
| MRR（平均倒数排名）| ≥ 0.70 | 衡量第一个相关结果的排名位置 |
| Answer Accuracy | ≥ 0.80 | Golden Set 上，回答与标准答案的语义匹配度（RAGAS 框架评估）|
| 无结果率 | ≤ 0.10 | 有答案的问题被误判为"未找到"的比例 |
| P95 响应延迟 | ≤ 8s | 单轮问答端到端延迟（含检索+生成）|
| 首 token 延迟 | ≤ 1s | 流式输出首字符出现时间，影响感知体验 |
| 用户满意度（点赞率）| ≥ 0.70 | 回答获得点赞的比例 |
| 降级触发率 | ≤ 0.05 | 降级链触发比例，超过 5% 需排查根因 |

---

*文档持续更新。下一步：技术详设（API 接口定义 + 数据库 Schema + Qdrant Collection 设计）*
