# M3 OCR Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** PDF 上传后，若某页文本提取量过少，自动走阿里云 OCR 兜底，对调用方透明。

**Architecture:** OCR 触发只在 `parsers/pdf.py` 内部。每页先用 pypdf 提取文本，文本 < 阈值时用 pypdfium2 渲染该页为 JPEG，调阿里云 `RecognizeGeneral` API 拿文本。单页失败仅 warning + 跳过，不阻塞整篇入库。阿里云 key 未配则模块禁用，行为完全等同 M2。

**Tech Stack:** Python 3.11+, pypdf 5.x（已有）, pypdfium2 4.30+, alibabacloud_ocr_api20210707 3.x, pytest 8.x, unittest.mock.

**Spec:** `docs/specs/2026-06-05-m3-ocr-design.md`

---

## File Structure

**Create:**
- `rag-backend/app/services/ocr_service.py` — Aliyun SDK wrapper (`OcrError`, `ocr_enabled()`, `ocr_image()`)
- `rag-backend/tests/unit/test_ocr_service.py` — 4 mock tests
- `rag-backend/tests/unit/test_parser_pdf_ocr.py` — 4 integration tests
- `rag-backend/tests/unit/fixtures/_generate_scanned_pdf.py` — 一次性 fixture 生成脚本（提交以便重生成）
- `rag-backend/tests/unit/fixtures/scanned.pdf` — 1 页无文本层 PDF（提交）
- `docs/m3-handoff.md` — M3 收尾文档

**Modify:**
- `rag-backend/pyproject.toml` — 加 2 个依赖
- `rag-backend/app/config.py` — 加 4 个配置项
- `rag-backend/app/services/parsers/pdf.py` — 加 OCR fallback 分支
- `rag-backend/.env.example` — 加占位
- `rag-backend/.env` — 加占位（本地，gitignored）

---

## Task 1: 依赖 + 配置项

**Files:**
- Modify: `rag-backend/pyproject.toml:24-32`
- Modify: `rag-backend/app/config.py`
- Modify: `rag-backend/.env.example`
- Modify: `rag-backend/.env`

- [ ] **Step 1.1: 加 OCR 相关 Python 依赖到 pyproject.toml**

修改 `rag-backend/pyproject.toml`，在 `# Retrieval rerank` 段下方插入 OCR 段：

```toml
    # Retrieval rerank
    "cohere>=5.13.0",

    # OCR (M3)
    "pypdfium2>=4.30.0",
    "alibabacloud_ocr_api20210707>=3.0.0",

    # Observability
```

- [ ] **Step 1.2: 安装依赖**

Run: `cd rag-backend && uv sync`
Expected: 输出包含 `+ pypdfium2` 和 `+ alibabacloud-ocr-api20210707` 两行，无错误。

- [ ] **Step 1.3: 验证 import 不报错**

Run: `cd rag-backend && uv run python -c "import pypdfium2, alibabacloud_ocr_api20210707; print('ok')"`
Expected: 输出 `ok`，无异常。若失败说明 Python 3.11+ 与阿里云 SDK 不兼容，采用 spec 风险条 1 的 fallback：临时改用 `httpx` 直调阿里云 HTTP API。

- [ ] **Step 1.4: 加配置项到 app/config.py**

在 `rag-backend/app/config.py` 中，于 `cohere_rerank_model` 字段之后追加：

```python
    # Aliyun OCR (optional; empty key = OCR disabled, falls back to M2 behavior)
    aliyun_ocr_access_key_id: str = Field("", alias="ALIYUN_OCR_ACCESS_KEY_ID")
    aliyun_ocr_access_key_secret: str = Field("", alias="ALIYUN_OCR_ACCESS_KEY_SECRET")
    aliyun_ocr_endpoint: str = Field(
        "ocr-api.cn-hangzhou.aliyuncs.com", alias="ALIYUN_OCR_ENDPOINT"
    )
    ocr_fallback_char_threshold: int = 50
```

- [ ] **Step 1.5: 更新 .env.example**

在 `rag-backend/.env.example` 末尾（`# App` 段之前）追加：

```
# Aliyun OCR (optional; empty = OCR disabled, scanned pages dropped)
ALIYUN_OCR_ACCESS_KEY_ID=
ALIYUN_OCR_ACCESS_KEY_SECRET=
ALIYUN_OCR_ENDPOINT=ocr-api.cn-hangzhou.aliyuncs.com
```

- [ ] **Step 1.6: 更新本地 .env（仅占位，不填 key）**

在 `rag-backend/.env` 末尾追加（保持空 key，验证空 key 启动）：

```
ALIYUN_OCR_ACCESS_KEY_ID=
ALIYUN_OCR_ACCESS_KEY_SECRET=
ALIYUN_OCR_ENDPOINT=ocr-api.cn-hangzhou.aliyuncs.com
```

- [ ] **Step 1.7: 空 key 下启动冒烟（仅校验配置加载）**

Run: `cd rag-backend && uv run python -c "from app.config import get_settings; s = get_settings(); print('ocr_key_set=', bool(s.aliyun_ocr_access_key_id)); print('threshold=', s.ocr_fallback_char_threshold)"`
Expected:
```
ocr_key_set= False
threshold= 50
```

- [ ] **Step 1.8: Commit**

```bash
git add rag-backend/pyproject.toml rag-backend/uv.lock rag-backend/app/config.py rag-backend/.env.example
git commit -m "$(cat <<'EOF'
feat(m3): deps + config — pypdfium2 + Aliyun OCR SDK (T1)

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

注意：**不要 add `rag-backend/.env`**（gitignored）。

---

## Task 2: ocr_service 模块 + 4 个单元测试 (TDD)

**Files:**
- Create: `rag-backend/app/services/ocr_service.py`
- Create: `rag-backend/tests/unit/test_ocr_service.py`

- [ ] **Step 2.1: 写 4 个失败测试**

创建 `rag-backend/tests/unit/test_ocr_service.py`：

```python
import json
from unittest.mock import MagicMock, patch

import pytest

from app.services.ocr_service import OcrError, ocr_enabled, ocr_image


def _make_mock_response(content: str) -> MagicMock:
    """阿里云 OCR SDK 的返回结构：response.body.data 是 JSON string."""
    resp = MagicMock()
    resp.body.data = json.dumps({"content": content})
    return resp


@patch("app.services.ocr_service._client")
@patch("app.services.ocr_service.ocr_enabled", return_value=True)
def test_ocr_image_success(mock_enabled, mock_client):
    mock_client.return_value.recognize_general_with_options.return_value = (
        _make_mock_response("hello world")
    )

    text = ocr_image(b"\xff\xd8\xff\xe0fake-jpeg-bytes")

    assert text == "hello world"
    mock_client.return_value.recognize_general_with_options.assert_called_once()


@patch("app.services.ocr_service._client")
@patch("app.services.ocr_service.ocr_enabled", return_value=True)
def test_ocr_image_api_error_wrapped(mock_enabled, mock_client):
    mock_client.return_value.recognize_general_with_options.side_effect = RuntimeError(
        "Aliyun 5xx"
    )

    with pytest.raises(OcrError) as exc:
        ocr_image(b"jpeg-bytes")

    assert "Aliyun 5xx" in str(exc.value)


def test_ocr_enabled_with_key(monkeypatch):
    monkeypatch.setenv("ALIYUN_OCR_ACCESS_KEY_ID", "fake-id")
    monkeypatch.setenv("ALIYUN_OCR_ACCESS_KEY_SECRET", "fake-secret")
    from app.config import get_settings

    get_settings.cache_clear()
    assert ocr_enabled() is True
    get_settings.cache_clear()


def test_ocr_enabled_without_key(monkeypatch):
    monkeypatch.setenv("ALIYUN_OCR_ACCESS_KEY_ID", "")
    monkeypatch.setenv("ALIYUN_OCR_ACCESS_KEY_SECRET", "")
    from app.config import get_settings

    get_settings.cache_clear()
    assert ocr_enabled() is False
    get_settings.cache_clear()
```

- [ ] **Step 2.2: 运行测试，确认全失败**

Run: `cd rag-backend && uv run pytest tests/unit/test_ocr_service.py -v`
Expected: 4 个测试全部 FAIL/ERROR，错误类型为 `ModuleNotFoundError: No module named 'app.services.ocr_service'`。

- [ ] **Step 2.3: 写最小实现**

创建 `rag-backend/app/services/ocr_service.py`：

```python
import io
import json
import logging
from functools import lru_cache

from alibabacloud_ocr_api20210707 import models as ocr_models
from alibabacloud_ocr_api20210707.client import Client as OcrClient
from alibabacloud_tea_openapi import models as open_api_models
from alibabacloud_tea_util.models import RuntimeOptions

from app.config import get_settings

logger = logging.getLogger(__name__)


class OcrError(Exception):
    """OCR 调用失败的统一异常。"""


def ocr_enabled() -> bool:
    s = get_settings()
    return bool(s.aliyun_ocr_access_key_id and s.aliyun_ocr_access_key_secret)


@lru_cache
def _client() -> OcrClient:
    s = get_settings()
    config = open_api_models.Config(
        access_key_id=s.aliyun_ocr_access_key_id,
        access_key_secret=s.aliyun_ocr_access_key_secret,
        endpoint=s.aliyun_ocr_endpoint,
    )
    return OcrClient(config)


def ocr_image(image_bytes: bytes) -> str:
    """单张图像 → 文本；失败抛 OcrError。

    Args:
        image_bytes: JPEG 或 PNG 字节流。

    Returns:
        识别出的文本（空字符串若图像无文字）。

    Raises:
        OcrError: SDK 调用失败 / 返回非预期结构 / 服务异常。
    """
    if not ocr_enabled():
        raise OcrError("OCR not enabled (Aliyun key missing)")

    request = ocr_models.RecognizeGeneralRequest(body=io.BytesIO(image_bytes))
    runtime = RuntimeOptions()
    try:
        resp = _client().recognize_general_with_options(request, runtime)
        data = json.loads(resp.body.data) if resp.body.data else {}
        return data.get("content", "") or ""
    except Exception as e:
        raise OcrError(str(e)) from e
```

- [ ] **Step 2.4: 运行测试，确认全通过**

Run: `cd rag-backend && uv run pytest tests/unit/test_ocr_service.py -v`
Expected: `4 passed`。若某个 mock 测试失败，逐项检查 patch 路径是否为 `app.services.ocr_service._client`（注意必须 patch 在使用处）。

- [ ] **Step 2.5: Commit**

```bash
git add rag-backend/app/services/ocr_service.py rag-backend/tests/unit/test_ocr_service.py
git commit -m "$(cat <<'EOF'
feat(m3): ocr_service Aliyun wrapper + 4 mock tests (T2)

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

## Task 3: pypdfium2 渲染 + scanned.pdf fixture

**Files:**
- Create: `rag-backend/tests/unit/fixtures/_generate_scanned_pdf.py`
- Create: `rag-backend/tests/unit/fixtures/scanned.pdf`

目的：生成一个 1 页**无文本层**的 PDF（页面是一张纯位图），用作 OCR 触发条件的 fixture。用 pypdfium2 自己造 PDF 可能麻烦，改用 pypdf + PIL 简洁方案。但 PIL/Pillow 不在依赖里，故采用纯 pypdfium2 + reportlab？也不在依赖里。

最简方案：**用 pypdf 构造一个空白页**（无文本、无图像），文本提取必然返回空字符串 → 满足 "len < 50 字符" 触发 OCR 的条件。该 fixture 不需要真实图像，因为 T4 测试中 OCR 调用是被 mock 的。

- [ ] **Step 3.1: 写 fixture 生成脚本**

创建 `rag-backend/tests/unit/fixtures/_generate_scanned_pdf.py`：

```python
"""Generate scanned.pdf fixture — a 1-page PDF with no extractable text.

This simulates a scanned document (image-only page) for OCR fallback testing.
The fixture does NOT contain a real image — tests mock OCR responses anyway.
What matters: pypdf.extract_text() returns empty string for this PDF, which
triggers the OCR code path.

Re-run with: uv run python tests/unit/fixtures/_generate_scanned_pdf.py
"""

from pathlib import Path

from pypdf import PdfWriter

OUT = Path(__file__).parent / "scanned.pdf"


def main() -> None:
    writer = PdfWriter()
    writer.add_blank_page(width=595, height=842)  # A4 in points
    with OUT.open("wb") as f:
        writer.write(f)
    print(f"wrote {OUT}")


if __name__ == "__main__":
    main()
```

- [ ] **Step 3.2: 运行脚本生成 fixture**

Run: `cd rag-backend && uv run python tests/unit/fixtures/_generate_scanned_pdf.py`
Expected: 输出 `wrote .../tests/unit/fixtures/scanned.pdf`，且 `ls tests/unit/fixtures/scanned.pdf` 显示文件已存在。

- [ ] **Step 3.3: 校验 fixture — 文本提取应为空**

Run: `cd rag-backend && uv run python -c "from pypdf import PdfReader; r = PdfReader('tests/unit/fixtures/scanned.pdf'); print('pages=', len(r.pages)); print('text=', repr(r.pages[0].extract_text()))"`
Expected:
```
pages= 1
text= ''
```
（或非常短的字符串。关键是 `len(text.strip()) < 50` 成立。）

- [ ] **Step 3.4: 校验 pypdfium2 能渲染该页**

Run: `cd rag-backend && uv run python -c "
import pypdfium2 as pdfium
pdf = pdfium.PdfDocument('tests/unit/fixtures/scanned.pdf')
page = pdf[0]
bitmap = page.render(scale=200/72)
pil = bitmap.to_pil()
import io
buf = io.BytesIO()
pil.save(buf, format='JPEG')
print('jpeg bytes=', len(buf.getvalue()))
"`
Expected: 输出 `jpeg bytes= N`，N 为正整数（白页 JPEG 一般 5-50KB）。若报错说明 pypdfium2 渲染链有问题，**这是 spec 风险条 2 的发生点**。

注意：上面命令依赖 Pillow（pypdfium2 的 to_pil() 走 PIL）。若 Pillow 未安装，T4 实现里会改用 `bitmap.to_numpy()` 路径，但通常 pypdfium2 会自动带上 Pillow。若缺，运行 `uv add pillow` 加进 pyproject 的 OCR 段。

- [ ] **Step 3.5: Commit**

```bash
git add rag-backend/tests/unit/fixtures/_generate_scanned_pdf.py rag-backend/tests/unit/fixtures/scanned.pdf
git commit -m "$(cat <<'EOF'
test(m3): scanned.pdf fixture (blank page, no text layer) + generator (T3)

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

## Task 4: parsers/pdf.py 集成 OCR fallback + 4 集成测试 (TDD)

**Files:**
- Modify: `rag-backend/app/services/parsers/pdf.py`
- Create: `rag-backend/tests/unit/test_parser_pdf_ocr.py`

- [ ] **Step 4.1: 写 4 个失败测试**

创建 `rag-backend/tests/unit/test_parser_pdf_ocr.py`：

```python
from pathlib import Path
from unittest.mock import MagicMock, patch

from app.services.parsers.pdf import parse_pdf

SCANNED = Path(__file__).parent / "fixtures" / "scanned.pdf"


@patch("app.services.parsers.pdf.PdfReader")
@patch("app.services.parsers.pdf.ocr_image")
@patch("app.services.parsers.pdf.ocr_enabled", return_value=True)
def test_text_pdf_skips_ocr(mock_enabled, mock_ocr, mock_reader):
    """页面文本 > 50 字符时，不应触发 OCR。"""
    long_text = (
        "The quick brown fox jumps over the lazy dog repeatedly today "
        "and tomorrow and forever amen."
    )
    mock_page = MagicMock()
    mock_page.extract_text.return_value = long_text
    mock_reader.return_value.pages = [mock_page]

    pages = parse_pdf(SCANNED)

    assert len(pages) == 1
    assert "fox" in pages[0]["text"].lower()
    assert pages[0]["page_num"] == 1
    assert pages[0]["kind"] == "page"
    mock_ocr.assert_not_called()


@patch("app.services.parsers.pdf.ocr_image", return_value="recognized scan content here")
@patch("app.services.parsers.pdf.ocr_enabled", return_value=True)
def test_scanned_pdf_triggers_ocr(mock_enabled, mock_ocr):
    pages = parse_pdf(SCANNED)
    assert len(pages) == 1
    assert pages[0]["page_num"] == 1
    assert pages[0]["text"] == "recognized scan content here"
    mock_ocr.assert_called_once()
    # The first positional arg should be JPEG/PNG bytes
    img_bytes = mock_ocr.call_args.args[0]
    assert isinstance(img_bytes, bytes)
    assert len(img_bytes) > 0


@patch("app.services.parsers.pdf.ocr_image")
@patch("app.services.parsers.pdf.ocr_enabled", return_value=True)
def test_ocr_failure_returns_empty_page(mock_enabled, mock_ocr):
    from app.services.ocr_service import OcrError

    mock_ocr.side_effect = OcrError("aliyun timeout")

    pages = parse_pdf(SCANNED)
    # Failed page is dropped (empty text → not yielded)
    assert pages == []
    mock_ocr.assert_called_once()


@patch("app.services.parsers.pdf.ocr_image")
@patch("app.services.parsers.pdf.ocr_enabled", return_value=False)
def test_ocr_disabled_when_no_key(mock_enabled, mock_ocr):
    pages = parse_pdf(SCANNED)
    assert pages == []
    mock_ocr.assert_not_called()
```

- [ ] **Step 4.2: 运行测试，确认失败**

Run: `cd rag-backend && uv run pytest tests/unit/test_parser_pdf_ocr.py -v`
Expected: 4 个测试都 FAIL。失败模式应包含：
- `test_text_pdf_skips_ocr`: AttributeError 因为 `parsers/pdf.py` 还没 import `ocr_image`/`ocr_enabled`，无法被 patch
- `test_scanned_pdf_triggers_ocr`: pages 长度 0（M2 行为：无文本页被丢）
- `test_ocr_failure_returns_empty_page`: pages 长度 0 但 ocr_image 未被调

若 patch 路径错（`app.services.parsers.pdf.ocr_image` 不存在），先跳到 Step 4.3 让模块出现这俩 import。

- [ ] **Step 4.3: 修改 parsers/pdf.py 加 OCR fallback**

完全替换 `rag-backend/app/services/parsers/pdf.py` 内容为：

```python
import io
import logging
from pathlib import Path

import pypdfium2 as pdfium
from pypdf import PdfReader

from app.config import get_settings
from app.services.ocr_service import OcrError, ocr_enabled, ocr_image

logger = logging.getLogger(__name__)


def _render_page_to_image(source: bytes | str | Path, page_index_zero_based: int) -> bytes:
    """Render a single PDF page to JPEG bytes via pypdfium2.

    Args:
        source: PDF bytes / path / Path.
        page_index_zero_based: page index, 0-based.

    Returns:
        JPEG bytes.

    Raises:
        Exception: any pypdfium2 / PIL render error (caller catches).
    """
    if isinstance(source, (bytes, bytearray)):
        pdf = pdfium.PdfDocument(bytes(source))
    else:
        pdf = pdfium.PdfDocument(str(source))

    page = pdf[page_index_zero_based]
    bitmap = page.render(scale=200 / 72)  # 200 dpi
    pil = bitmap.to_pil()
    buf = io.BytesIO()
    pil.save(buf, format="JPEG", quality=85)
    return buf.getvalue()


def parse_pdf(source: bytes | str | Path) -> list[dict]:
    """Returns list of {page_num, text, kind: 'page'}.

    Per-page flow:
      1. Try pypdf text extraction.
      2. If text < threshold AND OCR enabled, render page via pypdfium2 and call OCR.
      3. On OCR error or render error: log warning, treat page as empty (dropped).
    """
    threshold = get_settings().ocr_fallback_char_threshold
    use_ocr = ocr_enabled()

    if isinstance(source, (bytes, bytearray)):
        reader = PdfReader(io.BytesIO(source))
    else:
        reader = PdfReader(str(source))

    pages: list[dict] = []
    for i, page in enumerate(reader.pages, start=1):
        text = (page.extract_text() or "").strip()

        if len(text) < threshold and use_ocr:
            try:
                img_bytes = _render_page_to_image(source, i - 1)
                text = (ocr_image(img_bytes) or "").strip()
            except OcrError as e:
                logger.warning("OCR fail page=%d: %s", i, e)
                text = ""
            except Exception as e:
                logger.warning("PDF render fail page=%d: %s", i, e)
                text = ""

        if text:
            pages.append({"page_num": i, "text": text, "kind": "page"})
    return pages
```

- [ ] **Step 4.4: 运行测试，全通过**

Run: `cd rag-backend && uv run pytest tests/unit/test_parser_pdf_ocr.py -v`
Expected: `4 passed`。

若 `test_scanned_pdf_triggers_ocr` 失败，常见原因：
- pypdfium2 渲染抛错被 Exception 块吃掉（参考 T3.4 排查）
- threshold 配置未生效（确认 `get_settings()` 返回 50）

- [ ] **Step 4.5: 跑全部单元测试，确认无回归**

Run: `cd rag-backend && uv run pytest tests/unit/ -v`
Expected: `21 passed` (M2 13 + M3 OCR 4 + M3 OCR-service 4)。

- [ ] **Step 4.6: Commit**

```bash
git add rag-backend/app/services/parsers/pdf.py rag-backend/tests/unit/test_parser_pdf_ocr.py
git commit -m "$(cat <<'EOF'
feat(m3): PDF parser OCR fallback via pypdfium2 + Aliyun (T4)

Per-page text extraction; if text < 50 chars and OCR enabled, render via
pypdfium2 at 200 dpi and call Aliyun RecognizeGeneral. Per-page failures
drop the page with a warning; never raise to caller.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

## Task 5: App 启动冒烟 + 集成验证

**Files:** 无新文件（只跑命令）。

- [ ] **Step 5.1: 空 OCR key 启动后端，验证不报错**

Run（后台）: `cd rag-backend && uv run uvicorn app.main:app --port 8000 &`
等 2 秒。
Run: `curl -s http://localhost:8000/health`
Expected: 输出 `{"status":"ok"}` 或类似健康响应（与 M1/M2 完全一致）。

Run: `pkill -f "uvicorn app.main:app"` 收尾。

- [ ] **Step 5.2: 跑全部单元测试做最终 gate**

Run: `cd rag-backend && uv run pytest tests/unit/ -v --tb=short`
Expected: `21 passed`，0 fail 0 error。

- [ ] **Step 5.3: 跑 ruff 检查代码风格**

Run: `cd rag-backend && uv run ruff check app/services/ocr_service.py app/services/parsers/pdf.py tests/unit/test_ocr_service.py tests/unit/test_parser_pdf_ocr.py`
Expected: `All checks passed!`。若有 lint 错，按提示修后再跑。

- [ ] **Step 5.4: Commit（若 Step 5.3 修了文件，否则跳过）**

```bash
git status -s
# 若有 modified 文件:
git add rag-backend/app/services/ocr_service.py rag-backend/app/services/parsers/pdf.py
git commit -m "$(cat <<'EOF'
style(m3): ruff fixes for ocr module (T5)

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

## Task 6: M3 Handoff 文档

**Files:**
- Create: `docs/m3-handoff.md`

- [ ] **Step 6.1: 写 m3-handoff.md**

创建 `docs/m3-handoff.md`：

```markdown
# M3 Handoff

> Date: 2026-06-05
> Status: **M3 代码完成 6/6 task, 待 E2E（需填阿里云 OCR key）**
> Spec: `docs/specs/2026-06-05-m3-ocr-design.md`
> Plan: `docs/plans/2026-06-05-m3-ocr-plan.md`

## 已交付（vs M2 增量）

| 能力 | M2 | M3 |
|---|---|---|
| PDF 扫描页 | 直接被丢 | 自动调阿里云 OCR 兜底 |
| 触发条件 | — | 单页提取 < 50 字符且 OCR 启用 |
| 失败策略 | — | 单页失败仅 warning + 跳过，不阻塞 |
| 单元测试 | 13 | 21 (+8 OCR) |
| 依赖增加 | — | pypdfium2 + alibabacloud_ocr_api20210707 |
| Docker 镜像增重 | — | < 100MB（待 build 测） |

## 完成的 6 个 task

- T1 ✅ 依赖 + 配置项 + .env.example
- T2 ✅ ocr_service 模块 + 4 mock tests
- T3 ✅ scanned.pdf fixture 生成（空白页，文本层为空）
- T4 ✅ parsers/pdf.py 集成 OCR fallback + 4 集成 tests
- T5 ✅ 空 key 启动冒烟 + 21/21 测试 + ruff 通过
- T6 ✅ 本文档

## 你要做的 E2E 验收

```bash
# 1. 申请阿里云 OCR
# - https://www.aliyun.com/product/ocr-service
# - 开通 "通用文字识别"
# - 拿到 AccessKey ID/Secret

# 2. 填到 rag-backend/.env
ALIYUN_OCR_ACCESS_KEY_ID=LTAI...
ALIYUN_OCR_ACCESS_KEY_SECRET=...
# ALIYUN_OCR_ENDPOINT 不动（默认杭州；用香港则改 ocr-api.cn-hongkong.aliyuncs.com）

# 3. 启动
make dev &
cd rag-frontend && npm run dev &

# 4. 准备一个真实扫描 PDF（如扫描合同/老政策文件）
# 5. 浏览器
# - http://localhost:3000/documents 上传，等 status → indexed
# - 检查后端日志：应看到对该 PDF 调 RecognizeGeneral
# - http://localhost:3000 提问扫描件内容，应能答出并带 citation
```

## 已知问题 / M4 / M3.5 候选

按演示价值排：

1. **删除文档 + 重建索引**（管理体验）— 1 天 — **M3.5 推荐**
2. **演示品牌可配（白标）**（视觉加分）— 0.5 天 — **M3.5 推荐**
3. **多用户/团队隔离**（决定能否对外卖）— 3-5 天 — M4 / M5
4. **OCR 结果缓存**（按 SHA + page_num 缓存）— 1 天 — 量起来再做
5. **本地 BGE rerank**（成本/隐私）— 2 天 — 待客户隐私要求出现再做
6. **表格结构识别（OCR + table extract）** — 3-5 天 — 高价值但风险高

## 信心评级

| 项 | 信心 | 备注 |
|---|---|---|
| 编译 / 启动 | ⭐⭐⭐⭐⭐ | 21/21 tests + 启动冒烟通过 |
| 单元测试 | ⭐⭐⭐⭐⭐ | 全绿 |
| OCR 触发逻辑 | ⭐⭐⭐⭐⭐ | mock 覆盖了所有分支 |
| 真实阿里云 OCR 调用 | ⭐⭐⭐ | SDK 路径未实跑，依赖 E2E |
| 中文扫描准确率 | ⭐⭐⭐ | 阿里云通用 OCR 公开评测中文 99%+，但你的合同字体未知 |
| pypdfium2 Docker 兼容 | ⭐⭐⭐ | 本机通过，Linux 镜像未测 |

## Git 标签建议

E2E 跑通后：

```bash
git tag m3-done-$(date +%Y%m%d)
```

## M3.5 速通建议（若 OCR 验收顺利）

下一轮 ~1.5 天的"小而美"打包：

- 删除文档接口 + 前端按钮 + 级联清 chunk + 级联清 embedding
- 白标：`NEXT_PUBLIC_BRAND_NAME` / `NEXT_PUBLIC_BRAND_COLOR` 两个 env 驱动 page header/title
- 一并打 m3.5-done 标签
```

- [ ] **Step 6.2: Commit**

```bash
git add docs/m3-handoff.md
git commit -m "$(cat <<'EOF'
docs(m3): handoff — OCR fallback shipped, E2E pending (T6)

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

- [ ] **Step 6.3: 终态校验**

Run: `git log --oneline -7`
Expected: 看到 6 个 m3 commit + 之前的 spec commit + m2-handoff commit。

Run: `git status -s`
Expected: 空输出（工作区干净）。

---

## 总验收 Checklist

- [ ] 21/21 单元测试通过（M2 13 + M3 8）
- [ ] 空 OCR key 启动 backend 不报错，行为完全等同 M2
- [ ] ruff 通过
- [ ] 6 个 commit + 1 spec commit 干净落在 main
- [ ] `docs/m3-handoff.md` 提交
- [ ] （E2E，需用户）真实扫描 PDF 上传后能被问答引用
